let axios = require("axios");

module.exports = {
	
}
// (await axios.get(`https://v3.blox.link/developer/discord/${discordId}?guildId=${guildId}`, { headers: { 'api-key': process.env.BLOXLINK_API_KEY } })).data;